{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "RAILPACK"
  },
  "deploy": {
    "preDeployCommand": "npm --prefix backend ci && npm --prefix backend run build && npx --prefix backend prisma migrate deploy",
    "startCommand": "node backend/dist/server.js",
    "healthcheckPath": "/health"
  }
}
