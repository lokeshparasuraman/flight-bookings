[phases.setup]
nixPkgs = ["nodejs_18"]

[phases.install]
cmds = [
  "node backend/scripts/update-db-provider.js",
  "cd backend && npm install"
]

[phases.build]
cmds = [
  "cd backend && npm run build"
]

[start]
cmd = "cd backend && npx prisma db push && node dist/server.js"

